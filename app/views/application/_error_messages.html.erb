<% if object.errors.any? %>
  <div id="error_explanation">
    <span>So close! But the details weren't quite complete:</p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>